spring:
  cloud:
    zookeeper:
      config:
        enabled: true
        root: config
        defaultContext: application
        profileSeparator: ','
        watcher:
          enabled: false
  profiles:
    active: dev